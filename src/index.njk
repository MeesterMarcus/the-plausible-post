---
layout: "base.njk"
title: The Plausible Post - Your trusted news source for everything and nothing at once
description: The Plausible Post is a satirical news site delivering humorous takes on current events, pop culture, and everyday life. Stay informed and entertained with our witty articles and sharp commentary.
---
<section class="hero container">
  <h2 class="lead">Latest Headlines</h2>
</section>
 
<div class="section-separator container"></div>
 
<section class="home-grid container">
  <main class="articles-main">
    <section
      class="article-list"
      data-infinite-list
      data-infinite-initial="8"
      data-infinite-step="4"
    >
      {% for post in collections.articles %}
      <article class="teaser" data-article>
        <div class="teaser-content">
          <h3 class="teaser-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>

          {% if post.data.description %}
          <p class="teaser-lede">{{ post.data.description }}</p>
          {% else %}
          <p class="teaser-lede">{{ post.templateContent | striptags | decodeHtmlEntities | truncate(200, true) }}</p>
          {% endif %}

          <p class="teaser-meta"><time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time></p>
          <p class="teaser-author">{{ post.data.author or "Staff Writer" }}</p>
        </div>

        {% if post.data.image %}
        <figure class="teaser-media">
          <a href="{{ post.url }}"><img src="{{ post.data.image }}" alt="{{ post.data.title }}" /></a>
        </figure>
        {% endif %}
      </article>
      {% else %}
      <p>No articles yet.</p>
      {% endfor %}
    </section>
    <div class="infinite-status" data-infinite-status aria-live="polite" hidden>
      Loading more stories…
    </div>
    <noscript>
      <p class="infinite-noscript">For the full list of headlines, please enable JavaScript or use the site search.</p>
    </noscript>
    <script src="/assets/js/infinite-home.js" defer></script>
  </main>

  <aside class="sidebar">
    {% include "editors-picks.njk" %}
  </aside>
</section>
