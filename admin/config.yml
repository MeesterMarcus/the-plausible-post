backend:
  name: git-gateway
  branch: master
  # Optional: customize commit messages the CMS uses when committing via Git Gateway
  commit_messages:
    create: "Create {{collection}} \"{{slug}}\""
    update: "Update {{collection}} \"{{slug}}\""
    delete: "Delete {{collection}} \"{{slug}}\""
  # If you prefer GitHub OAuth instead of Netlify Git Gateway, switch back to `github` backend.

media_folder: "src/assets/images"
public_folder: "/assets/images"

# Use "simple" publish mode so Publish/Unpublish toggles are regular commits that update
# the frontmatter `published` flag instead of using Editorial Workflow which may attempt
# to move/delete files. This makes "Unpublish" flip published: false without deleting.
publish_mode: simple

collections:
  - name: "articles"
    label: "Articles"
    folder: "src/articles"
    create: true
    delete: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    format: "frontmatter"
    extension: "md"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "text", required: false}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Author", name: "author", widget: "string"}
      - {label: "Tags", name: "tags", widget: "list", required: false}
      - {label: "Image", name: "image", widget: "image", required: false}
      - {label: "Layout", name: "layout", widget: "hidden", default: "article.njk"}
      - {label: "Disclaimer", name: "disclaimer", widget: "string", required: false}
      - {label: "Published", name: "published", widget: "hidden", default: true}
      - label: "Body"
        name: "body"
        widget: "markdown"