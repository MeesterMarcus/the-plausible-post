backend:
  name: github
  repo: "MeesterMarcus/the-plausible-post"
  branch: main
  auth_scope: "repo"
  client_id: "Ov23liPJ1aG0v87ceURQ"
  # If you use an external identity provider (Auth0) as an OAuth proxy, set its tenant domain
  # in your hosting environment (not required by Decap itself). Example tenant:
  # AUTH0_TENANT_DOMAIN=dev-cxujhhifas4ifll7.auth0.com

media_folder: "src/assets/images"
public_folder: "/assets/images"

publish_mode: editorial_workflow

collections:
  - name: "articles"
    label: "Articles"
    folder: "src/articles"
    create: true
    delete: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    format: "frontmatter"
    extension: "md"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "text", required: false}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Author", name: "author", widget: "string"}
      - {label: "Tags", name: "tags", widget: "list", required: false}
      - {label: "Image", name: "image", widget: "image", required: false}
      - {label: "Layout", name: "layout", widget: "hidden", default: "article.njk"}
      - {label: "Disclaimer", name: "disclaimer", widget: "string", required: false}
      - {label: "Published", name: "published", widget: "boolean", default: true}
      - label: "Body"
        name: "body"
        widget: "markdown"